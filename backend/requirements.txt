# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Document parsing
pypdf>=4.0.0
python-docx>=1.1.0

# Environment and utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# LLM providers (for RLM)
anthropic>=0.75.0
openai>=2.14.0
google-genai>=1.56.0
portkey-ai>=2.1.0

# Rich output (for RLM)
rich>=13.0.0

# HTTP client for test script
requests>=2.31.0

# RLM is now in the same repo - install from root with:
# pip install -e ..
